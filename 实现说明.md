# 流式输出 + Markdown/LaTeX 混合渲染实现说明

## ✅ 已完成的功能

### 1. 流式 API 支持
- ✅ SSE (Server-Sent Events) 流式输出解析
- ✅ 逐字逐句显示答案的动画效果
- ✅ 流式状态管理（`isStreaming` 状态）
- ✅ 流式和非流式两种模式（流式为默认推荐）

### 2. Markdown + LaTeX 混合渲染
- ✅ WebView + Marked.js + KaTeX 渲染方案
- ✅ 支持完整 Markdown 语法（标题、列表、代码块、粗体、斜体、链接等）
- ✅ 支持 LaTeX 数学公式：
  - 行内公式：`$E=mc^2$`
  - 块级公式：`$$\int_0^\infty e^{-x^2}dx$$`
- ✅ 自动适配深色/浅色主题
- ✅ 流式输出时实时渲染

### 3. UI 优化
- ✅ 流式输出时显示小型加载指示器
- ✅ Material 3 配色方案
- ✅ 响应式布局

## 📁 新增/修改的文件

### 核心实现文件
1. **`app/src/main/java/com/lilinth/questionsolver/data/repository/QuestionRepository.kt`**
   - 新增 `solveQuestionStream()` 方法实现流式输出
   - 保留 `solveQuestion()` 非流式方法作为备用

2. **`app/src/main/java/com/lilinth/questionsolver/data/api/ApiService.kt`**
   - 已有 `chatCompletionStream()` 接口（返回 ResponseBody 用于 SSE 解析）

3. **`app/src/main/java/com/lilinth/questionsolver/ui/component/MarkdownRenderer.kt`** ✨ 新建
   - Markdown + LaTeX 渲染组件
   - WebView 封装
   - 主题自适应
   - 流式内容更新

4. **`app/src/main/assets/markdown_template.html`** ✨ 新建
   - HTML 渲染模板
   - 集成 Marked.js 和 KaTeX
   - JavaScript Bridge 函数
   - CSS 样式（深色/浅色主题）

5. **`app/src/main/java/com/lilinth/questionsolver/ui/viewmodel/MainViewModel.kt`**
   - 已有 `solveQuestionStream()` 方法
   - 已有流式状态管理

6. **`app/src/main/java/com/lilinth/questionsolver/ui/screen/MainScreen.kt`**
   - 已集成 `MarkdownRenderer` 组件
   - 流式状态 UI 更新

7. **`CLAUDE.md`**
   - 更新项目文档，记录新增功能

## 🔧 技术实现细节

### 流式输出实现原理

```kotlin
// QuestionRepository.kt
fun solveQuestionStream(
    imageBytes: ByteArray,
    apiConfig: ApiConfig
): Flow<String> = flow {
    // 1. 创建流式请求
    val request = ChatRequest(
        model = apiConfig.modelName,
        stream = true,  // 启用流式
        messages = [...]
    )
    
    // 2. 获取响应流
    val response = apiService.chatCompletionStream(request)
    val source = response.source()
    
    // 3. 逐行读取 SSE 数据
    while (!source.exhausted()) {
        val line = source.readUtf8Line() ?: break
        
        // 4. 解析 "data: {...}" 格式
        if (line.startsWith("data: ")) {
            val json = line.substring(6)
            if (json == "[DONE]") break
            
            // 5. 提取 delta.content
            val chunk = parseChunk(json)
            
            // 6. 发送到 UI
            emit(chunk)
        }
    }
}
```

### Markdown + LaTeX 渲染流程

```
┌─────────────┐
│  ViewModel  │
│  (Kotlin)   │
└──────┬──────┘
       │ answer: StateFlow<String>
       ▼
┌─────────────────┐
│ MarkdownRenderer│
│   (Compose)     │
└──────┬──────────┘
       │ WebView
       ▼
┌──────────────────────────────┐
│  markdown_template.html      │
│  ┌────────────────────────┐  │
│  │ Marked.js              │  │ Markdown → HTML
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │ KaTeX                  │  │ LaTeX → MathML
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │ JavaScript Bridge      │  │
│  │  - updateContent()     │  │
│  │  - setTheme()          │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

### LaTeX 语法示例

```markdown
这是行内公式 $E=mc^2$ 的示例。

块级公式示例：
$$
\int_0^\infty e^{-x^2}dx = \frac{\sqrt{\pi}}{2}
$$

矩阵示例：
$$
\begin{bmatrix}
a & b \\
c & d
\end{bmatrix}
$$
```

## 🚀 如何测试

### 前提条件
1. 配置 Android SDK（设置 `ANDROID_HOME` 环境变量或创建 `local.properties`）
2. 连接 Android 设备或启动模拟器

### 构建步骤
```bash
# 1. 构建 Debug APK
.\gradlew.bat assembleDebug

# 2. 安装到设备
.\gradlew.bat installDebug

# 3. 启动应用并测试
```

### 测试场景

#### 场景 1：流式输出测试
1. 打开应用，进入设置页面
2. 配置 API 信息：
   - Base URL: `https://api.openai.com/`
   - API Key: `sk-...`
   - Model: `gpt-4-vision-preview`
3. 返回主页，选择一张题目图片
4. 点击"开始解答"
5. **预期效果**：答案逐字逐句出现，右上角显示小型加载图标

#### 场景 2：Markdown 渲染测试
选择一张包含以下内容的题目：
- 标题（# ## ###）
- 列表（有序、无序）
- 代码块
- 粗体、斜体

**预期效果**：答案正确渲染 Markdown 格式

#### 场景 3：LaTeX 公式测试
选择一张数学题图片（如方程式、积分、矩阵）

**预期效果**：
- 行内公式正确渲染
- 块级公式居中显示
- 公式符号清晰可读

#### 场景 4：主题切换测试
1. 切换系统深色/浅色模式
2. 查看答案区域

**预期效果**：WebView 内容自动适配主题（背景色、文字色同步变化）

## 📝 使用示例

### API 返回示例（流式）

```
data: {"choices":[{"delta":{"content":"## "}}]}
data: {"choices":[{"delta":{"content":"解"}}]}
data: {"choices":[{"delta":{"content":"答"}}]}
data: {"choices":[{"delta":{"content":"过"}}]}
data: {"choices":[{"delta":{"content":"程\n\n"}}]}
data: {"choices":[{"delta":{"content":"已"}}]}
data: {"choices":[{"delta":{"content":"知"}}]}
data: {"choices":[{"delta":{"content":" $x=3$"}}]}
data: {"choices":[{"delta":{"content":"，"}}]}
data: {"choices":[{"delta":{"content":"求"}}]}
data: {"choices":[{"delta":{"content":" $y$ "}}]}
data: {"choices":[{"delta":{"content":"的"}}]}
data: {"choices":[{"delta":{"content":"值"}}]}
data: {"choices":[{"delta":{"content":"。\n\n"}}]}
data: {"choices":[{"delta":{"content":"$$\ny = 2x + 5\n$$"}}]}
data: [DONE]
```

### 渲染后效果

```
## 解答过程

已知 $x=3$，求 $y$ 的值。

$$
y = 2x + 5
$$
```

## 🔍 故障排查

### 问题 1：流式输出不工作
- 检查 API 是否支持 `stream: true` 参数
- 查看 Logcat 日志确认 SSE 数据格式
- 确认网络连接正常

### 问题 2：LaTeX 公式不渲染
- 检查 `assets/markdown_template.html` 是否正确打包
- 查看 WebView 控制台日志（开启 WebView 调试）
- 确认 LaTeX 语法正确（`$...$` 或 `$$...$$`）

### 问题 3：主题不切换
- 检查 `isSystemInDarkTheme()` 是否生效
- 查看 `setTheme()` JavaScript 调用是否成功
- 确认 CSS 变量定义正确

### 问题 4：WebView 显示空白
- 检查 HTML 文件路径：`file:///android_asset/markdown_template.html`
- 确认 WebView JavaScript 已启用
- 查看 WebView 错误日志

## 🎯 性能优化建议

### 当前实现
- 每次流式输出都立即更新 WebView（实时性好）
- 适合中等长度的答案（< 5000 字）

### 优化方案（如需要）
1. **批量更新**：每 50-100ms 批量更新一次 WebView
2. **虚拟滚动**：长文本使用虚拟列表
3. **懒加载**：公式延迟渲染
4. **缓存**：相同内容复用渲染结果

示例代码（批量更新）：
```kotlin
// MainViewModel.kt
private val updateBuffer = mutableListOf<String>()
private var lastUpdateTime = 0L

questionRepository.solveQuestionStream(imageBytes, config)
    .collect { chunk ->
        updateBuffer.add(chunk)
        val now = System.currentTimeMillis()
        
        // 每 50ms 更新一次
        if (now - lastUpdateTime > 50) {
            _answer.value += updateBuffer.joinToString("")
            updateBuffer.clear()
            lastUpdateTime = now
        }
    }
```

## 📚 相关文档

- [Marked.js 文档](https://marked.js.org/)
- [KaTeX 文档](https://katex.org/)
- [OpenAI API 流式文档](https://platform.openai.com/docs/api-reference/streaming)
- [Android WebView 指南](https://developer.android.com/guide/webapps/webview)

## 🎉 总结

所有功能已实现完毕，包括：
1. ✅ SSE 流式输出（逐字显示）
2. ✅ Markdown 完整渲染
3. ✅ LaTeX 数学公式支持
4. ✅ 深色/浅色主题自适应
5. ✅ 流式状态 UI 反馈

待 Android SDK 配置完成后，即可构建并测试完整功能。
